#ifndef _VERTICE_H
#define _VERTICE_H

#include <GL/freeglut.h>
#include <stdbool.h>
#include <vector>
#include <stdio.h>

// Struct de um ponto com 2 coordenadas(x,y).

class Ponto{
    private:
        double x;
        double y;
};

/*
typedef struct VERTICE
{
    double x,y;
}vertice;*/

#endif